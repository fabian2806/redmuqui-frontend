/**
 * Reflejo de `com.redmuqui.platform.common.catalog.dto.BaseCatalogoDTO`
 * en la forma JSON serializada por Jackson (accesores como propiedades).
 */
export interface BaseCatalogoDTO {
  id: number
  nombre: string
  descripcion: string | null
}
